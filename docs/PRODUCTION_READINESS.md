# Production readiness

Overall status: **NOT READY — bootstrap only**.

| Gate                                | Status                 | Evidence / blocker                                                           |
| ----------------------------------- | ---------------------- | ---------------------------------------------------------------------------- |
| Local profile slice                 | Complete for bootstrap | Batch 3 serialization/Unicode regressions passed; synthetic/loopback-only    |
| Real OIDC + PKCE                    | Blocked                | Provider not selected; only fail-closed local adapter exists                 |
| Authorization matrix                | Partial                | Owner predicates plus deterministic overlap/update/disable concurrency tests |
| Export/deletion/retention           | Blocked                | Design required before optional sensitive collection                         |
| Backup/restore and RPO/RTO          | Blocked                | No staging/production infrastructure                                         |
| Billing/entitlements                | Blocked                | No providers or production credentials selected                              |
| Logging/tracing/alerts              | Partial                | Matched-route/redaction tests and OTel hook; alert delivery untested         |
| ASVS/MASVS evidence                 | Partial                | Bootstrap evidence matrix and automation only                                |
| Dependency/SBOM provenance          | Promotion blocked      | Garage inventory plus release SBOM/signing/provenance evidence incomplete    |
| Mobile native security/store review | Blocked                | JavaScript export only; no signed native release or store declarations       |
| Legal/privacy review                | Blocked                | Qualified Indian/global review required                                      |
| Independent penetration test        | Blocked                | Required before public launch and cannot be self-attested                    |
| Staff MFA/access review             | Blocked                | No production organization/access plane                                      |
| Incident/breach drill               | Blocked                | Owners and production escalation paths not established                       |
| Critical/high findings              | Promotion blocked      | Waiver-free production scan exposes `CVE-2026-14456`; local waivers expire   |

## Bootstrap verification evidence

On 2026-08-29, the frozen install, environment doctor, loopback-preflighted setup/migration/seed, complete `verify` suite, security suite, metadata-validating smoke flow, and digest-pinned Linux Go race suite passed. PostgreSQL 18.6, Redis 8.2.9, and Garage 2.3.0 ran from immutable manifests and were healthy on `127.0.0.1`. Configuration tests reject hostnames, wildcard binds, and remote PostgreSQL/Redis/S3 endpoints; direct integration packages load the same validator before database/object mutations. Development authentication rejection for staging and production remains covered.

The full pnpm graph audit covered 1,224 dependencies and reported zero advisories. `govulncheck` and `gosec` reported no findings. Digest-pinned Gitleaks rejects a generated token canary, detects an unrelated token beside an exact local fixture value, detects a token retained only in a disposable committed repository's prior history using the real history command, and scans the working tree without any file-wide or path-wide credential exclusion. The current project has no `HEAD`, so it has no project history to inspect. Trivy inventoried 53 PostgreSQL and 22 Redis OS packages. Separate expiring `CVE-2026-14456` waivers are selected only by the exact local PostgreSQL and Redis digests; the waiver-free production scan mounts neither waiver and reports two unsuppressed findings in each image. Garage exposes no package inventory Trivy recognizes, so the local scan labels it missing coverage rather than clean. The executable production-image gate rejects those unwaived vulnerabilities, Garage's missing coverage and local-only status, and every image without verified release SBOM and publisher provenance.

Executable contract tests now compare every OpenAPI method/path/operation ID to the route table used by the real API mux and exercise each registered route. Generated-client tests assert `GET /v1/me` and `PATCH /v1/me`. The exact production middleware chain proves `/v1/me` retains its ServeMux route template through OTel while a real 404 is logged as unmatched. Builds inject the bootstrap version, a Git SHA when one exists, and UTC build time via linker flags; smoke verifies the exact `/version` body. This initial uncommitted repository has no `HEAD`, so local evidence correctly reports `commit=uncommitted`; CI is configured to inject `github.sha`.

The native Windows race command was not usable with the installed 32-bit MinGW compiler, so the same command passed in the immutable official Go 1.26.7 Linux image with a read-only source mount. GitHub Actions was not executed remotely because no commit or push was authorized. No DAST, complete static-binary dependency assessment, release SBOM/signature verification, native binary assessment, staging exercise, independent penetration test, legal review, backup/restore drill, or incident drill has been performed.

The 2026-08-29 final-review follow-up is complete for findings 1–8 without starting later milestones. Batch 1's pgx-effective target validation, exact root credential fixture, fail-closed Git inspection, and literal-loopback mobile credential controls remain intact. After Docker recovery, the trusted-directory secret scan and the later full security run both passed all pinned manifest/canary/unrelated/nested/history/working-tree/unborn-repository checks.

Batch 2 added a 10-second profile storage deadline beneath the unchanged 15-second server read/write limits and a stable single-write `504 profile_request_timeout` response. Profile updates now use the SQL `RETURNING` row and consents from one repeatable-read transaction. A real-PostgreSQL post-commit barrier test proves a later update or disable cannot replace or invalidate the committed response. Mobile component coverage proves dirty drafts and their original optimistic version survive newer background data and conflict invalidation. Node coverage executes the real workspace module from a copied path containing spaces and Unicode.

Targeted tests, mobile typecheck, 12/12 tooling tests, formatting, and generation-drift checks passed. `corepack pnpm run setup`, `verify`, `security`, and `smoke` exited 0; the full mobile suite passed 19/19 and the full real-container integration suite passed. The SHA-pinned Linux race gate passed with read-only source. The waiver-free `images:production-gate` exit 1 remains the expected promotion block: PostgreSQL and Redis each expose two `CVE-2026-14456` findings and lack release SBOM/provenance, while Garage remains local-only without vulnerability inventory or release SBOM/provenance.

The 2026-08-29 actionable P2 follow-up is complete without starting Prompt 1. Profile updates preserve their authoritative `UPDATE ... RETURNING` response and coherent repeatable-read transaction while detecting typed PostgreSQL SQLSTATE `40001`, retrying once, and returning the existing safe `409 version_conflict` rather than an internal error. A real handler integration test uses a snapshot synchronization barrier—no sleeps—to make two same-version PATCH operations overlap before either commits; it observes exactly one success, one defined conflict, three bounded transaction attempts, and a final winner at version 2. Mobile validation now counts Unicode code points consistently with Go and PostgreSQL, removes React Native's UTF-16 truncation cap, and covers 80/81 supplementary, mixed, and ASCII boundaries.

Focused handler tests exited 0; the focused real-PostgreSQL ownership/authoritative-response/overlap suite exited 0; focused mobile tests passed 9/9; and mobile typecheck exited 0. Formatting and its check exited 0. Generation drift exited 0 on the approved rerun after the initial sandbox-only pinned-tool network denial, with generated SQL/OpenAPI artifacts unchanged. The full `corepack pnpm run verify`, `security`, and `smoke` commands exited 0; `verify` passed 23/23 mobile tests plus the complete real-container and production-build path. Security again reported no secret, `govulncheck`, `gosec`, or pnpm audit findings, with 53 PostgreSQL and 22 Redis packages under exact local waivers and Garage still labeled missing coverage/local-only. Smoke reported `version=0.1.0-bootstrap`, `commit=uncommitted`, and `builtAt=2026-08-29T14:38:05.370Z`. The immutable Go 1.26.7 Linux `go test -race ./...` gate exited 0 with the repository mounted read-only; the first sandboxed full-verify attempt only failed to spawn Docker, and the approved rerun is green.

No public release may proceed until all master-prompt hard gates have current evidence and security/legal owners approve residual risk.
